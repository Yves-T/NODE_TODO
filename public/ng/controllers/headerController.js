(function () {

    'use strict';

    angular
        .module('todoApp')
        .controller('HeaderController', [
            '$auth',
            '$state',
            '$rootScope',
            function ($auth, $state, $rootScope) {

                var vm = this;
            }]);

})();